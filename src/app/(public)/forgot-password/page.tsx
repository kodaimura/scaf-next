import type { Metadata } from "next";
import ForgotPassword from "@/features/auth/ForgotPassword";

export const metadata: Metadata = { title: "パスワード再設定 | ScafNext" };

export default ForgotPassword;
