import type { Metadata } from "next";
import ChangePassword from "@/features/accounts/ChangePassword";

export const metadata: Metadata = { title: "パスワード変更 | ScafNext" };

export default ChangePassword;
